name: Studentkare CI Pipeline

on:
  push:
    branches: [ main, dev ]
  pull_request:
    branches: [ main, dev ]

jobs:
  build-and-test:
    name: Build, Audit & Test Validation
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Codebase
        uses: actions/checkout@v4

      - name: Setup Node.js Environment
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Setup Python Environment
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Frontend Dependencies
        working-directory: ./frontend
        run: npm ci

      - name: Frontend TypeScript Typecheck
        working-directory: ./frontend
        run: npx tsc --noEmit

      - name: Run Crisis Gate & Architecture Vitest Suite
        working-directory: ./frontend
        run: npx vitest run

      - name: Install Backend Dependencies
        working-directory: ./backend
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt pytest

      - name: Run Backend Security Pattern Pytest Suite
        working-directory: ./backend
        run: |
          PYTHONPATH=. pytest tests/test_data_security_pattern.py -v
